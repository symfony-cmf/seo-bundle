cmf_seo:
    persistence:
      phpcr: ~
    title:
        pattern: prepend
        separator: ' | '
        default: Default title
    content:
      pattern: canonical

cmf_routing:
    dynamic:
        enabled: true
        persistence:
            phpcr:
                route_basepath: /test/routes
                enabled: true
    chain:
        routers_by_id:
            cmf_routing.dynamic_router: 20
            router.default: 100
