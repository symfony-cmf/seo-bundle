{% extends "::base.html.twig" %}

<h1>Exception-Test</h1>

<div>
    <strong>{{ status_code }}</strong> {{ status_text }} - {{ exception.class|abbr_class }}
</div>
<div>
    <ul>
        {% for match in group, best_matches %}
            <li>
                {{ group }}
                {% for match in best_matches %}
                    {{ match.name }}
                {% endfor %}
            </li>
        {% endfor %}
    </ul>
</div>

